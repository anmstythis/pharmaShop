<Window x:Class="pharmaShop.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pharmaShop"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="AdminWindow" Height="450" Width="800" Background="#FFBCDEEE">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Frame x:Name="PageFrame"  Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" NavigationUIVisibility="Hidden"/>
        <Button x:Name="staffButton" Content="Сотрудники" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="staffButton_Click"/>
        <Button x:Name="roleButton" Content="Роли" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="roleButton_Click"/>
        <Button x:Name="accountButton" Content="Привязка аккаунта" Grid.Column="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="accountButton_Click"/>
    </Grid>
</Window>
